# ============================================================
# FutureKawa — Colombie — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs sensibles
# Ne jamais committer le fichier .env
# ============================================================

# Base de donnees PostgreSQL
POSTGRES_DB=futurekawa_colombie
POSTGRES_USER=futurekawa
POSTGRES_PASSWORD=change_me_in_production

# Broker MQTT (port expose sur la machine hote)
MQTT_PORT=1885

# API REST (port expose sur la machine hote)
API_PORT=8003

# Identite pays
PAYS=colombie
PAYS_CODE=CO

# Email — alertes
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_RESPONSABLE=responsable.colombie@futurekawa.com
